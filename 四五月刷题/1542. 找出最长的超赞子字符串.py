class Solution:
    def longestAwesome(self, s: str) -> int:
        """
        如果想重新组合一个回文字符串。
        需要满足回文串的特性：出现奇数次的字符个数小于等于 1。
        """
        sun = 1
        digital_flags = {str(key): 0 for key in range(0,10)}

        return sun

s = "1241"
Solution().longestAwesome(s)